# Database Configuration
AGENTS_DB_USER=agents
AGENTS_DB_PASSWORD=agents
AGENTS_DB_NAME=agents

# ADK Web Frontend Configuration
ADK_BACKEND_URL=http://host.docker.internal:6999

# React Chat Frontend Configuration  
NEXT_PUBLIC_API_URL=http://host.docker.internal:6999

# Optional: Set to production for optimized builds
NODE_ENV=development